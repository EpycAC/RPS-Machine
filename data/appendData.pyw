class storage():
    def setUp():
        import json
    def getProbabilities():
        info = open("probability.json", "r")
        probability = json.loads(info.read())
        print(probability[probability])
